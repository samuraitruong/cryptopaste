env:
  - CREATE_REACT_APP_API_URL=HTTPS://TEST
language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
# Handle git submodules yourself
git:
    submodules: false
# Use sed to replace the SSH URL with the public URL, then initialize submodules
#before_install:
#    - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
#    - git submodule update --init --recursive
script:
# - cd test-app
  - yarn
  - yarn test
  - yarn build
deploy:
  provider: pages
  all_branches: true
  skip_cleanup: true
  target_branch: ci-gh-pages
  github_token: $githubToken
  local_dir: build
  on:
  #  branch: master
    all_branches: true